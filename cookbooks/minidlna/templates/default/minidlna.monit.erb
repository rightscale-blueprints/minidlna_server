check process minidlna with pidfile /var/run/minidlna/minidlna.pid
  start program = "/usr/sbin/service minidlna start"
  stop program  = "/usr/sbin/service minidlna stop"
  if failed port 8200 with timeout 15 seconds for 3 cycles then restart
  if 3 restarts within 5 cycles then timeout
